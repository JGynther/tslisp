{
  "name": "tslisp",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "dev": "deno run --allow-read --import-map=import.json src/index.ts",
    "example": "pnpm dev 'example/hello.lisp'",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}