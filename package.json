{
  "name": "tslisp",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "dev": "deno run --import-map=import.json --unstable src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}